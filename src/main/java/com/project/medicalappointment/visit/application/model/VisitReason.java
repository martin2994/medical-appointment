package com.project.medicalappointment.visit.application.model;

/**
 * Enum which defines all visit reason
 */
public enum VisitReason {
    FIRST_VISIT,
    RECURRING_VISIT,
    URGENT_VISIT
}
